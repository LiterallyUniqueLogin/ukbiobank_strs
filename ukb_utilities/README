Even though all the utilities are symlinked here, they don't seem to work unless they are run
from the actual directory in which they are located
Also, these utilities require OS software that is too old on tscc - but they do work when 
run on Snorlax.
