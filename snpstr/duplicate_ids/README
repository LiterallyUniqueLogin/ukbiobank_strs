Lists of variants IDs which are present more than once
in the reference panel
File format is:
 #occurrences ID

produced with:

for chr in $(seq 1 22); do 
	cut -f3 ../vcf_1_sample/chr${chr}.vcf | sort | uniq -c | sort | awk '{ if ($1 != 1) print }' > \
		chr${chr}.txt
done

